GROUP=io.github.alims-repo
POM_ARTIFACT_ID=navease-gradle-plugin
# Local fallback only ? CI overrides this via ORG_GRADLE_PROJECT_VERSION_NAME
# which is derived automatically from the git tag (e.g. v0.0.8 ? 0.0.8).
VERSION_NAME=0.0.7
